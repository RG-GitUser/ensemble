import { LoadingScreen } from "@/components/LoadingScreen";

/** Renders inside the dashboard layout, so the sidebar stays put while the
    page streams in. */
export default function Loading() {
  return <LoadingScreen />;
}
